# --- Secrets (never commit these!) ---
.env
.env.*
!.env.example

# WhatsApp session credentials (contains creds.json — the live session!)
whatsapp_session/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# --- Node (vendored WhatsApp bridge) ---
whatsapp_bridge/node_modules/

# --- Models / runtime artifacts ---
models/
outputs/
*.gguf
*.log
